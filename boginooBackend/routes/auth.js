const express = require("express")
const {reqister, login} = require("../controller/auth");

const router = espress.Router();

router.post("/reqister", register), post("/login");

module.exports = router;
